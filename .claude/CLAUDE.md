skillは積極的に使うこと
私に忖度しないでください。常に批判的思考で対話し、私が間違っていると思ったら反論してください
あなたの変更内容に変更が加えられている場合、意図的に私が書き換えた可能性を考慮してください

## 文章表現

<!-- Claude Fable 5.1 向けの指示。出典: https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1 -->

- 気取った文章表現を避けてください
- 比喩や飾った表現で直接的な説明を置き換えないでください
- 文字通りの表現で言える場合は、そうしてください

## git

- **コミットメッセージは日本語で、ただしわかりやすさのために英語も併用してもよい**
  例: company テーブルに name カラムを追加した
- ブランチ名は feature, bugfix, refactor, chore のいずれかで始めること
<!-- - git add は禁止です。必要であればユーザーにフィードバックすること -->

## coding

- boolean 型の修飾子名は is,has,can,should または状態動詞の 3 人称単数現在形で始めること
- テストを修正するときは実装とテストのどちらを修正すべきか慎重に判断し、迷う場合はユーザーに確認すること
- コメントには後からコードを読む人に必要な最小限の情報だけを書き、作業中に試して外れた候補や調査の経過は書かない。ただし同じ罠を防ぐ否定形(「G は H の理由で使えないため E を使う」)は残してよい

### TypeScript

- `as any`は使用してはいけない。使用したくなったときはユーザーに判断を仰ぐ
  - `as`も極力使用しないこと

## 出力スタイル(視覚階層ルール)
  ターミナルは字サイズを変えられないため、**罫線・記号・余白**で iOS アプリ風の3階層を作る。長めの応答・レビュー・レポートに適用し、短い一問一答やmarkdownには使わない。
  ## レイヤー定義
  ### L1: タイトル(応答全体の主題 / 1応答につき1つまで)
  `┏━` 角付きの上下バーで挟み、タイトルは **中央寄せ**。センタリングは NBSP(U+00A0、半角スペースでは4つ以上だと code block 化する)で左パディングを作る。バー幅は 80
  文字固定(角 `┏┓` `┗┛` + `━` 78本)。
  書式:
  ```
  ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
                                    **タイトル**
  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
  ```
  中央寄せの計算: 表示幅 = ASCII 1 + 日本語 2。パディング幅 = (78 - 表示幅) / 2。パディングは NBSP で埋める。ズレが±1文字出ても可。
  ### L2: セクション(章見出し)
  `═` 二重線 80 本で上下を挟み、中に中央寄せの太字テキスト。記号は付けない。
  書式:
  ```
  ════════════════════════════════════════════════════════════════════════════════
                                   **セクション名**
  ════════════════════════════════════════════════════════════════════════════════
  ```
  ### L3: サブセクション(小見出し)
  `▎` 縦バー + 太字。装飾は最小限。**必ず前後に空行を1行ずつ入れる**(入れないと renderer が上下の間隔を不揃いに詰めることがある)。
  書式:
  ```
  ▎**サブ見出し**
  本文がすぐ下に続く。
  ```

